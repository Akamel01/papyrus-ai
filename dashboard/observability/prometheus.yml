# Prometheus scrape config for SME Dashboard
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'dashboard-backend'
    static_configs:
      - targets: ['sme_dashboard_api:8400']
    metrics_path: /metrics
    scrape_interval: 10s
